import styled from 'styled-components';

const Container = styled.div`
  position: fixed;
  left: 0;
  bottom: 0;
  width: 100%;
  color: transparent;
  text-align: center;
`;

export default Container;
